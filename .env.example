# ============================================
# CC-Bridge 配置文件
# ============================================

# --------------------------------------------
# 方式一：单 Telegram 渠道 (向后兼容)
# --------------------------------------------
# 如果你只需要一个 Telegram Bot，使用这种方式

# Telegram Bot Token (从 @BotFather 获取)
BOT_TOKEN=your_bot_token_here

# 授权的 Chat IDs (逗号分隔多个 ID)
CHAT_IDS=your_chat_id_here

# --------------------------------------------
# 方式二：多渠道配置
# --------------------------------------------
# 如果你需要多个渠道或多个 Bot，使用这种方式

# 启用的渠道列表 (逗号分隔，支持 telegram, feishu)
# CHANNELS=telegram,feishu

# =========== Telegram 配置 ===========
# 第一个 Telegram Bot
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token
# TELEGRAM_CHAT_IDS=chat_id_1,chat_id_2

# =========== 飞书配置 ===========
# 飞书应用配置 (从飞书开放平台获取)
# FEISHU_APP_ID=cli_xxxxxxxxxxxx
# FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxx
# 接收消息的用户/群 ID (逗号分隔多个)
# ID 类型可以是: open_id, user_id, union_id, chat_id
# FEISHU_RECEIVE_IDS=ou_xxxxxxxxxxxx,oc_xxxxxxxxxxxx

# --------------------------------------------
# 配置说明
# --------------------------------------------
#
# Telegram:
#   1. 在 https://t.me/BotFather 创建 Bot 获取 BOT_TOKEN
#   2. 向 Bot 发送消息后，可通过 API 获取 CHAT_IDS
#
# 飞书 (WebSocket 长连接模式):
#   1. 在 https://open.feishu.cn 创建企业自建应用
#   2. 获取 App ID 和 App Secret
#   3. 配置应用权限:
#      - im:message:send_as_bot (发送消息)
#      - im:message (接收消息)
#   4. 开启事件订阅，选择 "使用长连接接收事件"
#   5. 发布应用版本并启用
#   6. 获取用户的 open_id 或群的 chat_id 作为 RECEIVE_IDS
#
# --------------------------------------------
# 渠道切换
# --------------------------------------------
#
# 使用 /channel 命令切换活跃渠道:
#   /channel         - 显示渠道选择菜单
#   /channel feishu  - 切换到飞书渠道
#   /channel telegram- 切换到 Telegram 渠道
#
# 活跃渠道特性:
#   - Hook 消息只发送到活跃渠道
#   - 所有渠道都可以接收 / 命令
#   - 只有活跃渠道的文本消息会发送到 tmux
#   - 这样可以在某个渠道失效时通过其他渠道切换
#
# 多渠道配置示例:
#   CHANNELS=telegram,feishu
#   TELEGRAM_BOT_TOKEN=123456:ABC...
#   TELEGRAM_CHAT_IDS=12345,67890
#   FEISHU_APP_ID=cli_xxx
#   FEISHU_APP_SECRET=xxx
#   FEISHU_RECEIVE_IDS=ou_xxx,oc_xxx
